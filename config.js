// Supabase project credentials. The anon key is safe to expose publicly:
// row-level security on the server is what protects each user's data.
window.SUPABASE_CONFIG = {
  url: "",
  anonKey: "",
};
